services:
  - type: web
    name: stock-scanner-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python api_server_start.py
    envVars:
      - key: USE_MOCK_DATA
        value: "0"
      - key: ALLOW_DATA_FALLBACK
        value: "0"
      - key: TIMEZONE
        value: Asia/Seoul
      - key: KR_BACKTEST_MAX_SYMBOLS
        value: "60"
