name: Android Build (moved)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Stock Android app moved to shopper12/gpt_coin_android. Use Unified Trading Coach APK."
