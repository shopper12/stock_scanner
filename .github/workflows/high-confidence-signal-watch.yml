name: High Confidence Signal Watch (moved)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  moved:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Moved to shopper12/gpt_coin_android .github/workflows/unified-strategy-monitor.yml"
